// Project-level shared build settings.
// Overridable on a per-developer basis via Local.xcconfig (gitignored).

PRODUCT_BUNDLE_IDENTIFIER = com.local.in0
MARKETING_VERSION = 0.1.1
CURRENT_PROJECT_VERSION = 1

DEVELOPMENT_TEAM =
CODE_SIGN_STYLE = Automatic
CODE_SIGN_IDENTITY = -

#include? "Local.xcconfig"
